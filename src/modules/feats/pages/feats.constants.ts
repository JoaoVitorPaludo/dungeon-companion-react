import { BookOpen, Target, type LucideIcon } from "lucide-react";

export const FEATS_ICONS: Record<string, LucideIcon> = {
  grappler: Target,
};

export const FEATS_FALLBACK_ICON: LucideIcon = BookOpen;
