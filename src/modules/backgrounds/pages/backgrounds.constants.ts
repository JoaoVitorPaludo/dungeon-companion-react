import { BookOpen, type LucideIcon } from "lucide-react";

export const BACKGROUNDS_ICONS: Record<string, LucideIcon> = {
  acolyte: BookOpen,
};

export const BACKGROUNDS_FALLBACK_ICON: LucideIcon = BookOpen;
